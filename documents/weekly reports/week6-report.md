What have I done this week?
- Tried to get the neural network to learn, this included lot of strategies like gradient clipping, implementing a target net to improve numerical stability, implementing a replay buffer to balance learning, tried clipping activations to prevent Q values and target from exploding, changing learning parameters, reward shaping and so on.
How has the program progressed?
- Program has progressed with fine tuning the learning loop in hopes to get the neural net to learn.
What did I learn this week / today?
- Learned alot about what might be causing learning difficulties for the neural network, getting the algorithm to work but reinforcement learning is an art form in itself. There is no guarentees on learning and lots of things must be tried and tweaked and changed and see if the neural network starts to learn.
What remained unclear or caused difficulties?
- The self coded neural network is not performant, hence training games take a very long time so changing parameters and retraining takes alot of time which makes trouble shooting difficult. It might be that the net would simply need hours and days of learning time, which is difficult to give as the learning parameters might be off. Is lack of learning a time problem or fundamental algorithmic problem?
What will I do next?
- I have tried all possible changes and combinations of changes from activation clipping, network weight clipping, Q-value clipping, changing learning parameters, implementing replay buffer, implementing target network, changing rewards and their sizes but the neural network is still struggling to learn. Without replay buffer the network scored average score of around 85 per game which is suboptimal and might be due to some learned reward hacking instead of solid strategy. At this point all that can be done has been done for the duration and scope of this course project. Next improvement trials would be to try to change the state representation of the game to give the agent more awareness of the game and then run training for long period of time on a cluster. Before that, some profiling and code optimization would also be beneficial to achieve faster training. 
